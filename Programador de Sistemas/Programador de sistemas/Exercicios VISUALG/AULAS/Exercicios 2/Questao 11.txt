Algoritmo "semnome"
//11)Escreva um algoritmo que lê dois números inteiros,
//sendo o segundo maior que o primeiro, e imprime todos os
//números primos existentes entre eles.imprime todos os números primos
//existentes entre eles.
//a) Determine a soma dos números primos
//b) Determine a quantidade de números primos encontrada
//c) Determine a soma de números pares encontrados no intervalo
Var
   n1, n2: inteiro
   cont, aux, soma, quant, primos:inteiro
   spar, par:inteiro

Inicio

   escreval(" ")
   escreval("-----------------------------")
   escreval(" ")
   escreval("Digite o primeiro número: ")
   leia(n1)
   escreval("Digite o segundo número que seja maior que o primeiro: ")
   leia(n2)

   para cont de n1 até n2 passo 1 faça
      primos <- 0
      para aux de 1 até cont passo 1 faça
         se (cont % aux = 0) então
            primos <- primos + 1
         fimse
      fimpara
      se (primos <= 2) então
         escreval ("Número Primo: ", cont)
         soma <- soma + cont
      fimse
      se cont % 2 = 0 entao
      //escreval(cont)
         par<- par+1
         spar <- cont+spar
         fimse

   fimpara
   quant <- 1
   quant <- quant + primos

   escreval(" ")
   escreval("-----------------------------")
   escreval("Letra A: ", soma)
   escreval("letra B: ", quant)
     escreval("letra c: ", spar)




Fimalgoritmo